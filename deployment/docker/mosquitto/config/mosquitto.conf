# Local development broker (Step 11). Anonymous access is fine on a
# localhost-only, non-production broker; Phase 2 hardware deployment should
# add a password file and TLS before this ever leaves a dev machine.
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest file /mosquitto/log/mosquitto.log
log_dest stdout
