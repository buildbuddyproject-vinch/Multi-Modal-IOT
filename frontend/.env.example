# Copy to .env (local dev) or set as a build-time env var on your host (e.g.
# Render Static Site "Environment" tab -- Vite bakes this in at BUILD time,
# not runtime, so changing it requires a rebuild/redeploy).
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_LIVE_MONITORING_POLL_SECONDS=5
