# Multi-Modal IoT and Deep Learning Framework for Early Prediction of Sepsis
# Python 3.11 target (TensorFlow 2.x is not yet compatible with Python 3.13+/3.14)
# Install with: pip install -r requirements.txt

# --- Core numerical / data ---
numpy>=1.26,<2.0
pandas>=2.1,<2.3
scipy>=1.11,<1.14
pyarrow>=15.0,<17.0             # parquet I/O for processed datasets

# --- Deep learning ---
tensorflow>=2.15,<2.17
keras-tuner>=1.4,<2.0          # Bayesian Optimization for hyperparameter search

# --- Classical ML (baselines / benchmarking) ---
scikit-learn>=1.3,<1.6
xgboost>=2.0,<2.2
lightgbm>=4.1,<4.4

# --- Explainability ---
shap>=0.44,<0.47
lime>=0.2.0.1

# --- Visualization (training/EDA plots -- the frontend is a separate React
# app in frontend/, see frontend/package.json for its own dependencies) ---
matplotlib>=3.8,<3.10
seaborn>=0.13,<0.14

# --- Reporting ---
reportlab>=4.1,<4.3            # server-side PDF clinical report generation (Step 12+)

# --- Backend / API ---
fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.31
pydantic>=2.6,<2.9
pydantic-settings>=2.2,<2.5

# --- Database ---
pymongo>=4.6,<4.9
motor>=3.4,<3.6                # async MongoDB driver for FastAPI
mongomock>=4.1,<4.3            # in-memory MongoDB for tests

# --- MQTT ---
paho-mqtt>=1.6,<2.0

# --- Alerting ---
# Telegram alerts (Step 10) go straight to the Bot HTTP API via httpx (already a
# dependency) rather than python-telegram-bot, which is async-only as of v21 and
# would be awkward to call from this codebase's synchronous FastAPI routes.
# Email alerts use the stdlib (smtplib/email) -- no extra dependency needed.
python-dotenv>=1.0,<1.1

# --- Auth / security ---
passlib[bcrypt]>=1.7,<1.8
bcrypt>=4.0,<4.1                # passlib 1.7.4 doesn't understand bcrypt>=4.1's API (unmaintained upstream)
python-jose[cryptography]>=3.3,<3.4

# --- Testing ---
pytest>=8.0,<8.4
pytest-cov>=4.1,<5.1
pytest-asyncio>=0.23,<0.24
httpx>=0.27,<0.28              # FastAPI TestClient dependency

# --- Dev tooling ---
black>=24.1,<24.9
flake8>=7.0,<7.2
jupyter>=1.0,<1.1
tqdm>=4.66,<4.67
